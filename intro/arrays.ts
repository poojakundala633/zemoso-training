const heros:string[] = []

heros.push("spiderman")

const heropower : Array<number>=[]
type user = {
    name:string
    isactive :boolean

}

const alluser:user[] = []
alluser.push({name:"pooja",isactive:true})

const MLModels:number[][]=[
    [255,255,255],
]